parameter:
  name: token
  in: header
  description: Token de acceso que permite el consumo de las API's, el cual se puede obtener en la API [/auth](#operation/auth).
  required: true
  schema:
    type: string
